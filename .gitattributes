# Default: normalize line endings to LF in the repository
* text=auto

# PowerShell scripts — use CRLF on Windows checkout so they execute correctly
*.ps1   text eol=crlf
*.psm1  text eol=crlf
*.psd1  text eol=crlf

# Shell scripts — always LF
*.sh    text eol=lf

# Markdown — LF
*.md    text eol=lf

# YAML / JSON / TOML — LF
*.yml   text eol=lf
*.yaml  text eol=lf
*.json  text eol=lf
*.toml  text eol=lf

# TypeScript / JavaScript — LF
*.ts    text eol=lf
*.tsx   text eol=lf
*.js    text eol=lf
*.jsx   text eol=lf

# Python — LF
*.py    text eol=lf

# Binary files — no conversion
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.pdf   binary
*.zip   binary
*.gz    binary
*.tar   binary
